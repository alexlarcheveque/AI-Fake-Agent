{
  "name": "realnurture-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build:landing && npm run build:app",
    "build:landing": "cd frontend/landing && npm install && npm run build",
    "build:app": "cd frontend/app && npm install && npm run build"
  },
  "workspaces": [
    "frontend/landing",
    "frontend/app",
    "backend"
  ],
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.1"
  }
}
